apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress-resource
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"W
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: puprate
          servicePort: 4200
